# CLAUDE.md

이 저장소의 운영 규칙은 `wiki-rules.md`를 따른다. 어떤 작업이든 시작 전에 그 파일을 읽는다.

## 이 도구의 역할

Claude Code는 **넣기(ingest)** 와 **묻기(query)** 를 담당한다. 점검(lint)은 요청이 있을 때만 한다.

## 위임 규칙

본체는 판단과 위임만 한다. 문서를 실제로 읽고 쓰는 작업은 서브에이전트에게 넘긴다.

- `raw/`에 새 파일이 있거나 사용자가 "반영해", "넣어줘"라고 하면 → `ingest` 서브에이전트에게 위임한다. 파일이 여러 개면 파일당 한 번씩 순서대로 위임한다.
- 사용자가 "점검해", "lint"라고 하면 → `lint` 서브에이전트에게 위임한다.
- 사용자가 위키 내용을 물으면 → 본체가 직접 `wiki/index.md`를 읽고 관련 문서 1~4개만 열어 답한다. 답을 위키에 저장할 가치가 있으면 사용자에게 제안한 뒤, 동의하면 `ingest` 서브에이전트에게 "이 내용을 lesson/concept 문서로 저장"을 위임한다.

## 작업 습관

- 시작할 때 `git pull`, 끝날 때 `git add -A && git commit && git push`. 커밋 메시지 규칙은 wiki-rules.md 4절과 6절을 따른다.
- 위키 전체를 훑지 않는다. 항상 인덱스에서 시작한다.
- 사용자에게 답할 때는 어느 위키 문서를 근거로 했는지 파일명을 밝힌다.
