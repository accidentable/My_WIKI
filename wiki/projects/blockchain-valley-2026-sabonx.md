---
title: SabonX, 신분증 사본 없이 월급 확인·소득 신고 (DID/VC)
type: project
created: 2026-09-22
updated: 2026-09-22
sources: [raw/done/blockchain-valley-2026-sabonx-readme.md, raw/done/blockchain-valley-2026-sabonx-apis.md, raw/done/blockchain-valley-2026-trust404-readme.md]
repo: https://github.com/accidentable/Trust404_th
tags: [블록체인, DID, VC, SD-JWT, 개인정보, 프로토타입]
---

# SabonX, 주민등록증, 안전하게

## 1. 개요

- **코드 저장소**: https://github.com/accidentable/Trust404_th — SabonX-iM-Challenge 폴더는 같은 코드의 iM 제출본 (출처: 로컬 git 원격, 2026-09-22 확인)

- 대회: **2026 AI Blockchain Challenge in Daegu** 제출용 프로토타입. 같은 결과물을 **TRUST404 Track 02** 해커톤 프로토타입으로도 제출했다(두 README가 동일 구현을 각 대회명으로 소개).
- 기간: 자료에 없음.
- 주제: 신분증 사본을 사업주에게 넘기지 않고, DID·VC로 필요한 정보만 증명해 월급 확인부터 소득 신고까지 연결하는 서비스.
- 결과(수상 여부): **자료에 없음.**
- 데모: `https://211-233-200-43.nip.io/` (앱은 `/app`), Sepolia 폐기 레지스트리 컨트랙트 `0x6c30f02f3f5e31a9a0616c5b9756d8498a6b66e5`.
- AI 활용: 초기 구현은 Claude Code, 현재 앱 UI·급여 승인·변경 요청·폐기 결과·테스트·문서 개선은 OpenAI Codex. 문제 정의와 시연 방향은 사람이 정하고 결과를 검토·수정.

> 참고: SabonX와 TRUST404는 별개 프로젝트가 아니라, **같은 SabonX 구현의 두 대회 제출본**이다. 차이는 배지(TRUST404 Track 02)와 로컬 실행 안내(제출 ZIP 폴더명·저장 키 안내)뿐이다.

## 2. 문제 정의

급여 처리를 위해 신분증 사본 전체를 넘기면 업무에 필요 없는 사진·주소까지 상대에게 간다. SabonX는 신분증 이미지 대신 발급기관이 서명한 자격증명(VC)을 쓰고, 필요한 항목만 제출하는 흐름을 제안한다.

- 근로자는 월별 급여 내역을 확인하고 직접 서명한다. 금액이 틀리면 정정 메시지를 보낸다.
- 이름은 선택 공개하고, 주민등록번호는 모의 국세청만 복호화하도록 JWE로 봉인한다.
- 사업주는 근로자 이름·급여 요청 상태·모의 접수 결과만 본다.
- 정보 변경으로 폐기된 VC는 서명이 유효해도 제출 단계에서 거절한다.

## 3. 접근

### 아키텍처(논리 역할)

모의 주민센터(Issuer) → SD-JWT VC 발급 → 근로자 브라우저 지갑(Holder). 사업주 화면이 월 급여를 요청하면 SabonX 요청 관리가 서명된 요청을 지갑에 보내고, 지갑은 선택 공개 + KB-JWT + 승인 JWT를 모의 국세청(Verifier)에 제출한다. 검증기는 Sepolia IssuerRegistry에 폐기 상태를 RPC로 조회한 뒤 모의 접수 결과를 돌려준다.

단, 데모에서는 요청 관리·발급기관·세무 검증기가 **하나의 Node.js/Express 프로세스**에서 돈다. 기관 키가 인프라 수준으로 격리된 구조는 아니다.

### 기술 스택

TypeScript, React, Vite, Node.js/Express, Solidity, Docker. 암호 연산은 `jose`, `@noble/curves`, `@sd-jwt/sd-jwt-vc`. 지갑 저장은 IndexedDB + `idb-keyval`. 체인 접근은 viem + Sepolia RPC. 배포는 Ncloud + Docker Compose + Caddy.

| 기술 | 역할 |
|---|---|
| `did:key` · Ed25519 | 공개키 기반 식별자와 전자서명 (DID 자체가 실명 확인은 아님) |
| SD-JWT VC | 기관 서명 + 홀더 공개키 결합, 항목별 선택 공개 |
| `cnf.jwk` · KB-JWT | 제시자가 VC에 묶인 키의 소유자임을 nonce·aud·sd_hash로 증명 |
| JWS · 승인 JWT | 특정 급여 요청과 제출 묶음에 대한 동의를 해시로 결합 |
| JWE · X25519 | 주민번호를 `ECDH-ES+A256KW`/`A256GCM`으로 봉인 |
| Solidity · Sepolia | 폐기 기록 쓰기와 제출 시 폐기 상태 읽기 |

개념 상세는 [[sd-jwt-selective-disclosure-jwe]], [[vc-key-binding-approval-jwt]], [[onchain-revocation-registry]] 참고.

### 데모 시나리오

근로자 **윤태호**, 사업장 **카페 온유**, 테스트 번호 `010-0000-1234`(전부 가상 데이터).
발급 → 9월 급여 950,000원 접수 → 10월 승진·인상 요청 → 이사로 주소 변경(기존 VC 온체인 폐기) → 이전 VC로 '무시하고 제출하기' 시 서버 폐기 검증에서 거절 → 재발급 후 같은 10월 요청 정상 접수.

'무시하고 제출하기'는 화면의 사전 제한만 건너뛴다. 서버의 서명·폐기 검증은 우회하지 않는다.

### 데이터 보관과 신뢰 경계

| 위치 | 보관 내용 |
|---|---|
| 브라우저 지갑 | 개인키, 현재 VC, 폐기 시연용 이전 VC |
| 서버 발급 메타데이터 | 공개키, 폐기 식별자, 만료 시각, 변경 상태, 폐기 이력 (전체 VC 사본은 없음) |
| 서버 업무 기록 | 급여 요청, 이름, 정정 메시지, 처리 상태, 최소 검증 결과, 서명된 접수증 |
| 모의 기관 데이터 | 데모 발급용 가상 신원정보와 기관 키 |
| Sepolia | 폐기 식별자와 상태·이벤트·트랜잭션 (이름·주소·주민번호 원문은 올리지 않음) |

제출된 VP·승인 토큰·복호화한 주민번호는 업무 기록에 저장하지 않는다. 다만 모의 기관 원천 데이터는 남으므로 '데모 서버 전체에 주민번호가 없다'고 주장하지 않는다.

### API 표면 (레거시 단계별 검증 흐름)

`raw/done/blockchain-valley-2026-sabonx-apis.md`는 역할별 엔드포인트를 단계별로 눌러 보는 흐름을 설명한다. 현재 앱(`server/payrollRoutes.ts`) 이전의 검증 데모 경로로 보인다(추정, README가 `/legacy/*` 경로와 단계별 검증 스크립트를 "기존 암호 검증 흐름 재현용"으로 유지한다고 적음). 이 흐름에서 검증자는 국세청이 아니라 **사장님**이다.

- `POST /api/issuer/issue`, 발급. 응답의 `plaintextRrnInCredential`은 자격증명 전체를 문자열 검색한 결과로 항상 `false`여야 한다.
- `POST /api/issuer/revoke`, 분실 신고. 폐기 레지스트리에 **인덱스 번호만** 기록하고 txHash·Etherscan 링크를 돌려준다.
- `GET /api/holder/:holderId` / `POST /api/holder/:holderId/present`, 지갑 확인과 제시 생성. `deny`로 요청받은 항목도 뺄 수 있어 최종 결정권은 지갑에 있다. (이 엔드포인트의 지갑은 내용 확인용으로 서버가 대신 든다. 폰 흐름 `/wallet`에서는 개인키가 폰 IndexedDB에만 있다.)
- `GET /api/verifier/:sessionId`, 내부 검사 11개(`issuer-trusted` `issuer-signature` `validity` `credential-type` `disclosure-digests` `kb-signature` `kb-nonce` `kb-audience` `kb-sd-hash` `revocation` `library-crosscheck`)와 화면용 4줄 요약. `withheldDigests`로 미제출 항목은 다이제스트만 남는다.
- `POST /api/verifier/:sessionId/open`, **항상 실패하는** 엔드포인트. 사장님 개인키로도, 국세청 공개키로도 봉인이 열리지 않음을 보여주는 것이 목적이다.
- `POST /api/tax/unseal`, 국세청 개인키로 봉인을 열어 일용근로소득 지급명세서를 만든다. 세액은 0원이어도 명세서에는 주민번호 13자리가 필요하다는 점이 이 데모의 논거다.
- 그 외: `GET /api/chain`(브라우저가 직접 RPC를 읽도록 레지스트리 설정 제공), `GET /api/revocation/:index`, `POST /api/merchant`, `POST /api/session`, `POST /api/session/:id/vp`.

## 4. 잘된 점 / 안된 점

### 잘된 점
- 새 암호 알고리즘을 만들지 않고, SD-JWT/JWE/KB-JWT/온체인 폐기 각각의 **보장 범위를 구분해** 하나의 업무 요청에 결합했다.
- 데모가 "막혀야 할 것이 실제로 막히는지"를 보여준다: 이전 VC 제출 거절, 봉인 열기 실패 엔드포인트, 미제출 항목의 다이제스트 노출.
- 서버 VC 캐시를 제거해 지갑 밖에 전체 VC 사본이 남지 않게 했다.
- 통합 테스트(`npm run test:payroll`)가 발급·선택공개·접수, 잘못된 키·승인 변조·중복 제출 거절, 정정 메시지, 권한 거부, 이전 VC 거절/재발급 접수, 시계 오차 허용 범위를 확인한다.

### 안된 점 / 한계 (문서가 스스로 밝힌 범위)
- 기관 키가 같은 프로세스에 있어 인프라 수준 키 격리가 없다.
- 브라우저 내장 지갑이라 사이트 코드가 개인키를 읽을 수 있다. XSS·악성 배포에 대한 하드웨어 수준 보호 없음. 외부 지갑 연동은 미구현.
- 서버 상태가 메모리 Map이라 재시작하면 새 데모를 시작해야 하고, 다중 인스턴스에는 DB 트랜잭션이 필요하다.
- 기관 신원 확인, 이름·주소 변경 승인, 전화번호 라우팅, 세무 접수는 전부 모의. 실제 SMS·푸시·송금·세액 계산·납부 없음. 알림은 폴링.
- 요청은 데모당 50건, 세션 24시간·서버당 최대 200개.
- 기기 변경·키 복구, 실서비스 인증·인가, 공식 기관 연동과 법적 효력은 범위 밖. 키까지 잃으면 새 세션이 필요하다.
- 로컬 스텁 테스트 통과가 실제 Sepolia 쓰기 성공을 뜻하지 않는다.

## 5. 재사용 가능한 것

- `src/issuer`, `src/holder`, `src/verifier`, `src/tax`: 발급·제시·검증·복호화 모듈
- `src/shared/revocation.ts`: 온체인 및 로컬 스텁 폐기 레지스트리 (체인 환경변수가 없으면 **LOCAL STUB** 사용)
- `contracts`: Solidity 컨트랙트와 Foundry 테스트
- `scripts/payroll-test.ts`: 현재 앱 통합 테스트
- `src/ui/pages/PayApp.tsx`, `src/ui/pages/PayReview.tsx`: 역할별 앱과 급여 확인·선택 공개·승인 슬라이드 UI
- `server/payrollRoutes.ts`: 현재 앱 API
- 운영 패턴: Docker Compose + Caddy(HTTPS) 앱 갱신 절차, `/api/chain`으로 `isStub` 확인
- 환경변수 규약: `ISSUER_SEED`/`TAX_SEED`(base64url 32바이트), `CHAIN_ID`(Sepolia `11155111`), `RPC_URL`, `REGISTRY_ADDRESS`, `ISSUER_CHAIN_KEY`, `SITE_ADDRESS`/`ACME_EMAIL`
- 문서 구성: [frontend README 템플릿](https://github.com/yewon-Noh/readme-template/tree/main/frontend)을 따른 소개·화면 구성·기술 스택·기술적 이슈 구조

## 6. 관련 문서

- [[sd-jwt-selective-disclosure-jwe]], 선택적 공개와 봉인 암호화를 함께 쓰는 이유
- [[vc-key-binding-approval-jwt]], 키 바인딩과 업무 승인을 결합하기
- [[onchain-revocation-registry]], 온체인 폐기 레지스트리와 fail-closed 검증
- [[testnet-reward-token]], 같은 EVM 테스트넷 컨트랙트를 쓴 다른 프로젝트([[hana-ar-kowalk-2026]])
- [[im-challenge-daegu-2026-sns-menu]], 같은 2026 AI Blockchain Challenge in Daegu에 낸 다른 트랙 결과물(소상공인·골목상권 분야)

## 면접 준비

### 한 문장 소개

급여를 받으려고 신분증 사본을 통째로 사장님에게 넘기면 업무와 상관없는 사진과 주소까지 같이 간다는 문제를 잡고, 발급기관이 서명한 SD-JWT VC에서 이름만 골라 공개하고 주민등록번호는 모의 국세청만 열 수 있게 JWE로 봉인해 내는 급여 확인·소득 신고 흐름을 만들었습니다. 정보가 바뀌어 폐기된 VC는 서명이 멀쩡해도 Sepolia 폐기 레지스트리 조회에 걸려 제출 단계에서 거절되는 것까지 데모로 보여 드립니다.

### 기술 스택과 선택 이유

| 기술 | 역할 | 왜 이걸 썼나 | 대안과 포기한 것 |
|---|---|---|---|
| TypeScript · React · Vite | 랜딩과 iPhone 형태 앱, 급여 확인·선택 공개·승인 슬라이드 UI | 이유 자료에 없음 | 이유 자료에 없음 |
| SD-JWT VC (`@sd-jwt/sd-jwt-vc`) | 기관 서명 + 홀더 공개키 결합, 항목별 선택 공개 | 신분증 이미지 대신 기관이 서명한 자격증명을 쓰고 필요한 항목만 내게 하려고 골랐습니다 | 신분증 사본을 건네는 방식을 버렸습니다. 다만 SD-JWT는 공개하기로 한 값을 암호화하지 않아 따로 보호가 더 필요했습니다 |
| JWE · X25519 (`jose`) | 주민번호를 `ECDH-ES+A256KW`/`A256GCM`으로 봉인 | 세무 처리로 넘어가는 주민번호에는 선택적 공개와 별개로 기밀성이 필요했습니다. 일용근로소득 지급명세서에는 세액이 0원이어도 주민번호 13자리가 들어갑니다 | 주민번호를 아예 빼는 안은 명세서 요건 때문에 포기했습니다 |
| `cnf.jwk` · KB-JWT | 제시자가 VC에 묶인 키의 소유자임을 nonce·aud·sd_hash로 증명 | 발급기관 서명만 보면 지금 낸 사람이 정당한 소유자인지 알 수 없었습니다 | VC 복사와 다른 키 서명은 막지만 개인키 자체를 빼앗기는 경우는 못 막습니다 |
| JWS 승인 JWT | `request_id`·`request_hash`·`presentation_hash`·`decision`을 묶어 서명 | 신원 제시가 유효하다고 해서 특정 월·금액에 동의한 것은 아니고, 요청과 제시 묶음을 바꿔치기하는 것도 막아야 했습니다 | 제시 서명 한 번으로 끝내는 단순함을 포기했습니다. 금액이 달라진 다음 달 요청에는 새 승인이 필요합니다 |
| Solidity · Sepolia · viem | 폐기 기록 쓰기, 제출 시 `revoked(statusIndex)` 읽기 | 주소가 바뀌어도 이전 VC의 서명은 수학적으로 멀쩡해서, 화면 버튼만 막아서는 서버가 폐기를 확인했다고 할 수 없었습니다 | 메인넷 대신 테스트넷을 썼습니다. 조회는 `eth_call`이라 가스가 들지 않지만 RPC 제공자는 조회하는 식별자를 볼 수 있습니다 |
| IndexedDB · `idb-keyval` | 브라우저 지갑에 개인키와 VC 보관 | 서버에 발급 결과를 캐시하면 지갑 밖에도 전체 VC 사본이 남아서, 캐시를 없애고 메타데이터만 서버에 남겼습니다 | 외부 지갑 연동을 포기했습니다. 사이트 코드가 개인키를 읽을 수 있어 XSS나 악성 배포에 대한 하드웨어 수준 보호가 없습니다 |
| Express · 메모리 Map | 역할 권한, 급여 요청, 정정 메시지, 접수 결과, 발급 메타데이터 | 이유 자료에 없음 | 영속 DB를 포기했습니다. 재시작하면 데모를 새로 시작해야 하고 다중 인스턴스에는 DB 트랜잭션이 필요합니다 |

### 아키텍처
- [client] 근로자 지갑 :: React, Vite, IndexedDB, idb-keyval :: 근로자 브라우저
- [client] 사업주 화면 :: React, Vite :: 사업주 브라우저
- [edge] Caddy :: Caddy, HTTPS :: Ncloud, Docker Compose
- [server] SabonX 서버 :: Node.js, Express, TypeScript :: Ncloud, Docker Compose
- [data] 메모리 저장소 :: Node Map :: SabonX 서버 프로세스 내부
- [external] Sepolia RPC :: viem :: 외부 RPC 제공자
- [chain] IssuerRegistry :: Solidity :: Sepolia 테스트넷
- 근로자 지갑 -> Caddy :: HTTPS/JSON
- 사업주 화면 -> Caddy :: HTTPS/JSON
- Caddy -> SabonX 서버 :: 역방향 프록시
- SabonX 서버 -> 메모리 저장소 :: 급여 요청, 발급 메타데이터, 접수 결과
- SabonX 서버 -> Sepolia RPC :: revoked(statusIndex) eth_call
- 근로자 지갑 -> Sepolia RPC :: /api/chain 설정으로 폐기 상태 직접 조회
- Sepolia RPC -> IssuerRegistry :: 폐기 기록 읽기와 쓰기

### 고민한 점

- **선택적 공개만으로 충분한가** — SD-JWT는 숨길 항목을 가려 주는데 공개하기로 한 값을 암호화하지는 않아서, 주민번호를 그대로 보내면 검증자에게 평문이 갑니다. 그렇다고 빼면 지급명세서를 만들 수 없어서, 공개 범위는 선택적 공개가 맡고 전달 값의 기밀성은 JWE가 맡도록 역할을 갈라 함께 썼습니다. 암호문은 발급기관 서명이 보호하는 `rrn_sealed`에 넣었고, 사장님 개인키로도 국세청 공개키로도 열리지 않는다는 것을 보이려고 항상 실패하는 엔드포인트(`POST /api/verifier/:sessionId/open`)를 일부러 남겨 뒀습니다.
- **서버 VC 캐시를 없앨 것인가** — 캐시를 두면 지갑 밖에 전체 사본이 남고, 없애면 발급 응답을 잃었을 때 기존 VC를 다시 내려줄 수 없어 한참 망설였습니다. 캐시를 없애고 `Cache-Control: no-store`에 재발급(기존 증명을 폐기하고 새로 발급)을 붙이는 쪽으로 갔는데, 해 보니 서버 사본 동기화 걱정은 사라졌지만 키까지 잃으면 지금 데모에서는 새 세션을 시작해야 하는 값을 치렀습니다.
- **폐기 조회가 실패하면 접수할 것인가** — RPC 장애로 폐기 상태를 못 읽었을 때 통과시키면 폐기 검증이 의미를 잃습니다. 조회가 실패해도 접수하지 않는 fail-closed로 정하되, 폐기와 네트워크 오류를 화면에서 갈라 보여 주고 로컬 스텁일 때는 온체인으로 확인했다고 표시하지 않기로 했습니다.
- **실기기 시계 차이** — 실기기 시연에서 브라우저의 `iat`가 서버 시각보다 앞서 발급이 거절됐습니다. JWT 검증에 60초 Clock Tolerance를 넣었는데 그러면 시간 검증 자체가 헐거워질까 걱정돼서, 30초 미래 요청은 허용하고 과도한 미래나 만료된 요청은 거절하는 테스트를 같이 둬서 확인했습니다.
- **데모 편의와 검증 무결성** — 폐기 거절을 보여 주려면 사용자가 일부러 이전 VC를 낼 수 있어야 했습니다. '무시하고 제출하기'는 화면의 사전 제한만 건너뛰게 하고 서버의 서명·폐기 검증은 그대로 받게 했습니다.

### 예상 질문

1. **Q:** (L1) 이 서비스에서 "신분증 사본을 넘기지 않는다"는 것이 구체적으로 무슨 뜻인가요? 어떤 정보가 어디에 남는지 설명해 주시겠어요?
   **A:** 신분증 이미지 대신 발급기관이 서명한 VC를 쓰고, 제출할 때 이름 Disclosure만 공개하고 주민번호는 JWE로 봉인해 보냅니다. 브라우저 지갑에는 개인키와 VC가 남고, 서버 발급 메타데이터에는 공개키·폐기 식별자·만료 시각·변경 상태만 남으며 전체 VC 사본은 없습니다. Sepolia에는 폐기 식별자와 상태·이벤트만 올리고 이름·주소·주민번호 원문은 올리지 않습니다. 다만 데모 발급용 모의 기관 원천 데이터가 서버에 남아 있어서, "데모 서버 전체에 주민번호가 없다"고는 말하지 않습니다.
   **꼬리:** 제출된 VP와 복호화한 주민번호는 왜 업무 기록에 저장하지 않으셨나요? 처리 중 잠깐 들고 있는 것과 계속 보관하는 것을 어떻게 가르셨나요?
   **틀리기 쉬운 답:** "블록체인에 올리니까 안전하다"고 답하는 경우가 있는데, 실제로는 체인에 올라간 것이 폐기 식별자뿐이고 개인정보는 오히려 체인에 올리지 않는 것이 이 설계의 핵심입니다.
2. **Q:** (L2) 폐기 목록은 서버 DB에 둬도 될 것 같은데, 왜 굳이 체인에 올리셨나요?
   **A:** 폐기 기록을 여러 검증자가 같은 기준으로 확인할 수 있게 하려고 체인을 썼습니다. 발급기관 권한으로 `revoke(statusIndex)`를 보내고, 검증기는 제출 때마다 `revoked(statusIndex)`를 `eth_call`로 읽습니다. 솔직히 말씀드리면 이번 데모에서는 요청 관리·발급기관·검증기가 하나의 Node.js 프로세스에서 돌기 때문에 "여러 검증자"라는 이점이 실제로 살아난 상태는 아닙니다. 블록체인이 개인정보의 진실성이나 기관의 정직함까지 보장해 주지도 않습니다.
   **꼬리:** 기관이 악의로 멀쩡한 VC를 폐기해 버리면 이 방식은 무엇을 보장하나요?
   **틀리기 쉬운 답:** "탈중앙화라서 위변조가 불가능하다"로 끝내는 경우가 있는데, 실제로 체인이 보장하는 것은 폐기 기록을 함께 볼 수 있는 기준이지 기록 내용의 진실성이 아닙니다.
3. **Q:** (L2) 데모에서는 발급기관·요청 관리·검증기가 한 프로세스에 있는데, 그런데도 이 프로토타입이 무엇을 보였다고 말할 수 있을까요?
   **A:** 보인 것은 신뢰 경계의 분리가 아니라 암호 절차를 묶어도 실제로 돌아간다는 점입니다. 발급, 선택 공개, JWE 봉인, 키 바인딩, 승인 결합, 폐기 조회, 중복 제출 차단은 실제로 구현했고, 통합 테스트(`npm run test:payroll`)가 잘못된 키·승인 변조·중복 제출 거절과 이전 VC 거절 후 재발급 접수, 시계 오차 허용 범위까지 확인합니다. 기관 키가 인프라 수준으로 갈라져 있지 않다는 점은 README에 한계로 적어 뒀습니다.
   **꼬리:** 실제로 기관을 나누려면 무엇부터 바꾸시겠어요? 키 관리와 API 경계 중 어디가 먼저인가요?
   **틀리기 쉬운 답:** 아키텍처 그림의 역할 구분을 실제 격리인 것처럼 말하는 경우가 있는데, 실제로는 논리적 역할을 나눠 그린 것입니다.
4. **Q:** (L3) 이걸 실서비스로 올리면 어디가 먼저 문제가 될까요?
   **A:** 먼저 상태 저장입니다. 서버가 메모리 Map이라 재시작하면 데모를 새로 시작해야 하고, 요청은 데모당 50건, 세션은 24시간에 서버당 최대 200개로 묶여 있습니다. 인스턴스를 여러 개 띄우면 동시 제출 방지를 세션 잠금에서 DB 트랜잭션으로 옮겨야 합니다. 다음은 지갑인데, 브라우저 내장이라 사이트 코드가 개인키를 읽을 수 있고 기기 변경이나 키 복구 경로가 없습니다. 알림도 폴링이고 SMS·푸시·송금·세액 계산은 전부 모의입니다.
   **꼬리:** 키 복구를 넣으면 "지갑 밖에 전체 VC 사본을 두지 않는다"는 원칙과 부딪치는데 어떻게 타협하시겠어요?
   **틀리기 쉬운 답:** "DB만 붙이면 된다"고 답하는 경우가 있는데, 실제로는 동시성과 복구 설계에 키 복구까지 같이 와야 하고 키 복구는 개인정보 보관 원칙과 정면으로 부딪칩니다.

### 솔직하게 말할 것

- 초기 구현은 Claude Code가, 현재 앱 UI와 급여 승인·변경 요청·폐기 결과 화면, 테스트, 문서 개선은 OpenAI Codex가 짠 부분이라 제가 설명할 수 있는 것은 문제 정의와 시연 방향, 그리고 결과를 검토하며 고친 내용까지입니다. 암호 라이브러리(`jose`, `@noble/curves`, `@sd-jwt/sd-jwt-vc`)를 조합한 것이지 새 알고리즘을 만든 것이 아니라는 점도 먼저 말씀드립니다.
- 온체인 경로는 검증이 덜 됐습니다. 로컬 스텁 테스트가 통과했다고 해서 실제 Sepolia 쓰기가 성공한다는 뜻이 아니라고 README에 적어 뒀고, 환경변수가 없으면 LOCAL STUB으로 돕니다.
- 기관의 신원 확인, 이름·주소 변경 승인, 전화번호 라우팅, 세무 접수는 전부 모의라 제가 말할 수 있는 범위는 거기까지입니다. 실제 SMS·푸시·송금·세액 계산·납부는 없고, 공식 기관 연동과 법적 효력은 범위 밖입니다.
- `/legacy/*` 단계별 검증 API 흐름은 지금 앱(`server/payrollRoutes.ts`) 이전의 검증 데모 경로로 보이는데(추정) 확실하지 않습니다. 이 흐름에서는 검증자가 국세청이 아니라 사장님이라 역할 배치가 달라서, 두 흐름을 섞어 말하지 않도록 조심하고 있습니다.
- 대회 결과는 자료에 없습니다. 같은 구현을 2026 AI Blockchain Challenge in Daegu와 TRUST404 Track 02 두 곳에 냈고 두 README의 차이는 배지와 로컬 실행 안내뿐이라는 점도 밝히는 편이 낫다고 생각합니다.
