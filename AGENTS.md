# AGENTS.md

이 저장소의 운영 규칙은 `wiki-rules.md`를 따른다. 어떤 작업이든 시작 전에 그 파일을 읽는다.

## 이 도구의 역할

Codex는 Claude Code와 동등한 **넣기(ingest)와 묻기(query)** 담당이다. 점검(lint)은 사람이 명시적으로 요청할 때만 하고, 평소에는 `tools/lint.py`가 맡는다.

- `raw/`에 새 파일이 있거나 사용자가 "반영해", "넣어줘"라고 하면 wiki-rules.md 4절 절차를 그대로 수행한다. 파일이 여러 개면 하나씩 순서대로.
- 사용자가 위키 내용을 물으면 `wiki/index.md`를 먼저 읽고 관련 문서 1~4개만 열어 답한다. 어느 문서를 근거로 했는지 파일명을 밝힌다.
- 답이 다시 쓸 가치가 있는 종합이면 lesson/concept 문서로 저장할지 제안하고, 동의하면 저장한다.

## 다른 프로젝트 폴더에서 작업 중일 때

이 저장소 밖에서 해커톤 프로젝트를 코딩하다가 마무리 단계가 되면, 회고(무엇을 만들었고, 무엇이 잘 됐고 안 됐고, 재사용할 코드는 어디 있는지)를 마크다운으로 써서 `C:\Users\pc\Desktop\해커톤\WIKI\raw\`에 저장할지 사용자에게 제안한다. 파일명은 `<프로젝트>-<YYYY-MM>-retro.md`.

## 작업 습관

- 시작할 때 `git pull`, 끝날 때 `git add -A && git commit && git push`. 커밋 메시지는 wiki-rules.md 4절 형식. 메시지 끝에 `[codex]`를 붙인다.
- 위키 전체를 훑지 않는다. 항상 인덱스에서 시작한다.
- `raw/`는 읽기만 하고, 처리 후 `raw/done/`으로 옮기기만 한다.
