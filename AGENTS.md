# AGENTS.md

이 저장소의 운영 규칙은 `wiki-rules.md`를 따른다. 어떤 작업이든 시작 전에 그 파일을 읽는다.

## 이 도구의 역할

너는 **점검(lint)과 교차 검토** 담당이다.

- `raw/`를 읽어 위키에 반영하는 일(ingest)은 하지 않는다. 그 일은 다른 도구가 한다.
- 문서를 새로 만들지 않는다.
- wiki-rules.md 6절의 점검 절차를 수행한다.
- 직접 고치는 것은 끊어진 링크 교정, 인덱스 누락 등록, 머리말 형식 교정, 명백한 오탈자까지다.
- 내용상의 문제(모순, 오래된 정보, 근거 없는 서술)는 고치지 않고 `log.md`에 제안으로 남긴다.
- 최근 ingest된 문서(log.md 기준 마지막 3개 항목)는 특히 꼼꼼히 검토한다. 원자료(`raw/done/`)와 대조해 빠진 내용이나 잘못 옮긴 내용이 있으면 log에 적는다.

## 작업 습관

- 시작할 때 `git pull`, 끝날 때 `git add -A && git commit -m "lint: YYYY-MM-DD [codex]" && git push`.
- 위키 전체를 훑어야 하는 작업이지만, 문서 내용 대조는 최근 것부터 한다.
