# 해커톤 지식 위키 운영 규칙

이 저장소는 Ray의 해커톤 경험을 LLM이 정리하고 관리하는 위키다.
사람은 `raw/`에 자료를 넣고 질문을 한다. 문서를 쓰고 고치는 것은 LLM의 일이다.
어떤 도구(Claude Code, Codex 등)로 작업하든 이 파일의 규칙을 따른다.

## 1. 폴더 구조

```
raw/            사람이 넣는 원자료. LLM은 절대 수정하지 않는다.
raw/done/       처리가 끝난 원자료. 반영 후 여기로 옮긴다.
wiki/           LLM이 쓰는 위키 문서 (모두 마크다운)
wiki/index.md   전체 목차. 모든 문서는 여기 한 줄로 등록된다.
wiki/projects/  해커톤 프로젝트 하나당 문서 하나
wiki/concepts/  기술, 도구, 도메인 개념 문서
wiki/lessons/   여러 프로젝트에 걸친 교훈, 패턴, 실수 모음
log.md          작업 기록. 무엇을 언제 반영했는지 시간순으로 적는다.
```

## 2. 절대 규칙

- `raw/` 안의 파일은 읽기만 한다. 내용을 고치거나 지우지 않는다. 처리 후 `raw/done/`으로 이동만 한다.
- 원자료에 없는 사실을 지어내지 않는다. 추론한 내용은 "(추정)"이라고 표시한다.
- 문서를 만들거나 크게 고치면 반드시 `wiki/index.md`와 `log.md`를 같이 갱신한다.
- 작업 시작 전에 `wiki/index.md`를 먼저 읽는다. 위키 전체를 처음부터 다 읽지 않는다. 인덱스에서 관련 문서만 골라 연다.
- 서로 모순되는 서술을 발견하면 지우지 말고, 두 서술과 출처를 남기고 문서 상단에 `> ⚠️ 모순:` 블록으로 표시한다.

## 3. 문서 형식

모든 위키 문서는 이 머리말(frontmatter)로 시작한다.

```markdown
---
title: 문서 제목
type: project | concept | lesson
created: YYYY-MM-DD
updated: YYYY-MM-DD
sources: [raw/done/파일명, ...]
tags: [태그1, 태그2]
---
```

- 파일명은 영문 소문자와 하이픈만 쓴다. 예: `bc-card-bigdata-2026.md`, `time-series-anomaly.md`
- 제목(title)은 한국어로 써도 된다.
- 다른 문서로의 링크는 `[[파일명]]` 형식(확장자 없이)을 쓴다. 예: `[[time-series-anomaly]]`
- 본문은 한국어. 코드, 명령, 라이브러리명은 원문 그대로.
- 문서 하나는 한 주제만 다룬다. 길어지면 쪼개고 서로 링크한다.

### project 문서 구성
1. 개요: 대회명, 기간, 주제, 결과(수상 여부)
2. 문제 정의: 무엇을 풀려 했는가
3. 접근: 사용한 데이터, 모델, 기술 스택, 아키텍처
4. 잘된 점 / 안된 점
5. 재사용 가능한 것: 코드, 프롬프트, 데이터 전처리 등 (경로 포함)
6. 관련 문서 링크

### concept 문서 구성
1. 한 줄 정의
2. 어디서 썼는가 (project 문서 링크)
3. 실제로 겪은 문제와 해결
4. 참고 자료

### lesson 문서 구성
1. 교훈 한 문장
2. 근거가 된 사례 (project 문서 링크, 최소 1개)
3. 다음에 어떻게 적용할 것인가

## 4. 넣기 (ingest) 절차

`raw/`에 새 파일이 있을 때 수행한다.

1. `wiki/index.md`를 읽는다.
2. 원자료를 읽는다. URL만 적힌 파일이면 해당 페이지를 가져와 읽는다.
3. 이 자료가 건드리는 기존 문서를 인덱스에서 고른다. 보통 1~4개.
4. 기존 문서를 갱신하거나 새 문서를 만든다. 새 개념이 3개 이상 나오면 각각 concept 문서 후보로 삼되, 한 문단 이상 쓸 내용이 있을 때만 만든다.
5. 여러 프로젝트에 걸쳐 반복되는 패턴이 보이면 lesson 문서를 만들거나 갱신한다.
6. `wiki/index.md`에 새 문서를 한 줄로 등록하고, 갱신된 문서의 updated 날짜를 고친다.
7. `log.md` 맨 아래에 기록을 추가한다.
8. 원자료를 `raw/done/`으로 옮긴다.
9. git commit. 메시지: `ingest: <원자료 파일명>`

## 5. 묻기 (query) 절차

사람이 위키에 관해 질문할 때 수행한다.

1. `wiki/index.md`를 읽고 관련 문서를 고른다.
2. 고른 문서만 읽고 답한다. 위키에 없는 내용이면 "위키에 없음"이라고 분명히 말한다.
3. 답이 새로운 종합(여러 문서를 엮어 만든 결론)이고 다시 쓸 가치가 있으면, lesson 문서나 concept 문서로 저장할지 사람에게 제안한다. 저장하면 index와 log를 갱신한다.

## 6. 점검 (lint) 절차

주기적으로, 또는 요청 시 수행한다.

1. `wiki/` 안의 모든 `[[링크]]`가 실제 파일을 가리키는지 확인한다.
2. `wiki/` 안의 모든 문서가 `wiki/index.md`에 등록되어 있는지, 인덱스에 있는데 파일이 없는 항목은 없는지 확인한다.
3. 머리말이 규칙대로인지 확인한다.
4. 같은 주제를 다루는 문서가 둘 이상인지, 서로 모순되는 서술이 있는지 확인한다.
5. 발견 사항을 `log.md`에 `lint:` 항목으로 기록한다.
   - 링크 교정, 인덱스 누락 등록, 오탈자는 직접 고친다.
   - 내용 수정이 필요한 것은 고치지 않고 log에 제안으로만 남긴다.
6. git commit. 메시지: `lint: <날짜>`

## 7. 인덱스 형식 (`wiki/index.md`)

섹션(projects / concepts / lessons)별로, 문서당 한 줄만 쓴다.

```
- [[bc-card-bigdata-2026]] — BC카드 빅데이터 해커톤, 카드 결제 데이터 기반 상권 분석 (2026-08)
```

인덱스는 짧게 유지한다. 모든 작업이 인덱스를 먼저 읽으므로 이 파일이 길어지면 전체 비용이 오른다.

## 8. 로그 형식 (`log.md`)

맨 아래에 추가만 한다. 기존 항목은 고치지 않는다.

```
## 2026-09-22 ingest: bc-card-readme.md
- 신규: projects/bc-card-bigdata-2026
- 갱신: concepts/time-series-anomaly (이상탐지 절 추가)
- 도구: claude-code
```

## 9. 동시 작업 금지

두 컴퓨터 또는 두 도구가 동시에 위키를 고치면 충돌이 난다.
작업 시작 전 `git pull`, 끝나면 바로 `git commit` 후 `git push`한다.

## 10. 점검 스크립트와 노션 트래커

- 정기 점검은 `tools/lint.py`가 한다. 결정적 검사(링크·인덱스·머리말·sources)는 코드로, 판단 검사(중복·모순·원자료 대비 누락)는 OpenAI API로 하며, 결과는 `log.md`에 `lint:` 항목으로만 남기고 자동 수정은 하지 않는다.
- 대회 자체의 정보(기간, 주최, 상금, 결과, 상태)의 원본은 노션 트래커 "🏆 해커톤 - 현상금 사냥꾼"이다. 위키는 이 값을 project 문서 개요에 옮겨 적되, 출처를 "노션 트래커"라고 밝힌다.
- 반대로 "무엇을 만들었는가"의 원본은 위키다. 넣기가 끝나면 트래커의 해당 행에 프로젝트명, 기술 스택, 핵심 아이디어, 위키 문서 GitHub 링크(`https://github.com/accidentable/My_WIKI/blob/main/wiki/projects/<파일명>.md`)를 채운다. 위키 본문을 노션에 복사하지 않는다.
