# 해커톤 지식 위키 운영 규칙

이 저장소는 Ray의 해커톤 경험을 LLM이 정리하고 관리하는 위키다.
사람은 `raw/`에 자료를 넣고 질문을 한다. 문서를 쓰고 고치는 것은 LLM의 일이다.
어떤 도구(Claude Code, Codex 등)로 작업하든 이 파일의 규칙을 따른다.

## 1. 폴더 구조

```
raw/            사람이 넣는 원자료. LLM은 절대 수정하지 않는다.
raw/done/       처리가 끝난 원자료. 반영 후 여기로 옮긴다.
wiki/           LLM이 쓰는 위키 문서 (모두 마크다운)
wiki/index.md   전체 목차. 모든 문서는 여기 한 줄로 등록된다.
wiki/projects/  해커톤 프로젝트 하나당 문서 하나
wiki/concepts/  기술, 도구, 도메인 개념 문서
wiki/lessons/   여러 프로젝트에 걸친 교훈, 패턴, 실수 모음
log.md          작업 기록. 무엇을 언제 반영했는지 시간순으로 적는다.
```

## 2. 절대 규칙

- `raw/` 안의 파일은 읽기만 한다. 내용을 고치거나 지우지 않는다. 처리 후 `raw/done/`으로 이동만 한다.
- 원자료에 없는 사실을 지어내지 않는다. 추론한 내용은 "(추정)"이라고 표시한다.
- **원자료의 확신 수준을 그대로 보존한다.** 원자료가 "검토했다", "생각한다", "추정", "가능성"이라고 쓴 것을 "기각했다", "확인했다", "이다"로 바꾸지 않는다. 원자료가 밝힌 한계와 단서 조항(표본 제외, 기간 제약, 분리 불가 등)은 결론 문장과 같은 단락에 함께 옮긴다. 문장을 매끄럽게 다듬는 과정에서 조심스러움이 깎이는 것이 가장 흔한 왜곡이다.
- 문서를 만들거나 크게 고치면 반드시 `wiki/index.md`와 `log.md`를 같이 갱신한다.
- 작업 시작 전에 `wiki/index.md`를 먼저 읽는다. 위키 전체를 처음부터 다 읽지 않는다. 인덱스에서 관련 문서만 골라 연다.
- 서로 모순되는 서술을 발견하면 지우지 말고, 두 서술과 출처를 남기고 문서 상단에 `> ⚠️ 모순:` 블록으로 표시한다.

## 3. 문서 형식

모든 위키 문서는 이 머리말(frontmatter)로 시작한다.

```markdown
---
title: 문서 제목
type: project | concept | lesson
created: YYYY-MM-DD
updated: YYYY-MM-DD
sources: [raw/done/파일명, ...]
tags: [태그1, 태그2]
---
```

- 파일명은 영문 소문자와 하이픈만 쓴다. 예: `bc-card-bigdata-2026.md`, `time-series-anomaly.md`
- 제목(title)은 한국어로 써도 된다.
- 다른 문서로의 링크는 `[[파일명]]` 형식(확장자 없이)을 쓴다. 예: `[[time-series-anomaly]]`
- 본문은 한국어. 코드, 명령, 라이브러리명은 원문 그대로.
- 문서 하나는 한 주제만 다룬다. 길어지면 쪼개고 서로 링크한다.

### project 문서 구성
1. 개요: 대회명, 기간, 주제, 결과(수상 여부)
2. 문제 정의: 무엇을 풀려 했는가
3. 접근: 사용한 데이터, 모델, 기술 스택, 아키텍처
4. 잘된 점 / 안된 점
5. 재사용 가능한 것: 코드, 프롬프트, 데이터 전처리 등 (경로 포함)
6. 관련 문서 링크

### concept 문서 구성
1. 한 줄 정의
2. 어디서 썼는가 (project 문서 링크)
3. 실제로 겪은 문제와 해결
4. 참고 자료

### lesson 문서 구성
1. 교훈 한 문장
2. 근거가 된 사례 (project 문서 링크, 최소 1개)
3. 다음에 어떻게 적용할 것인가

## 4. 넣기 (ingest) 절차

`raw/`에 새 파일이 있을 때 수행한다.

1. `wiki/index.md`를 읽는다.
2. 원자료를 읽는다. URL만 적힌 파일이면 해당 페이지를 가져와 읽는다.
3. 이 자료가 건드리는 기존 문서를 인덱스에서 고른다. 보통 1~4개.
4. 기존 문서를 갱신하거나 새 문서를 만든다. 새 개념이 3개 이상 나오면 각각 concept 문서 후보로 삼되, 한 문단 이상 쓸 내용이 있을 때만 만든다.
5. 여러 프로젝트에 걸쳐 반복되는 패턴이 보이면 lesson 문서를 만들거나 갱신한다.
6. `wiki/index.md`에 새 문서를 한 줄로 등록하고, 갱신된 문서의 updated 날짜를 고친다.
7. `log.md` 맨 아래에 기록을 추가한다.
8. 원자료를 `raw/done/`으로 옮긴다.
9. git commit. 메시지: `ingest: <원자료 파일명>`

## 5. 묻기 (query) 절차

사람이 위키에 관해 질문할 때 수행한다.

1. `wiki/index.md`를 읽고 관련 문서를 고른다.
2. 고른 문서만 읽고 답한다. 위키에 없는 내용이면 "위키에 없음"이라고 분명히 말한다.
3. 답이 새로운 종합(여러 문서를 엮어 만든 결론)이고 다시 쓸 가치가 있으면, lesson 문서나 concept 문서로 저장할지 사람에게 제안한다. 저장하면 index와 log를 갱신한다.

## 6. 점검 (lint) 절차

주기적으로, 또는 요청 시 수행한다.

1. `wiki/` 안의 모든 `[[링크]]`가 실제 파일을 가리키는지 확인한다.
2. `wiki/` 안의 모든 문서가 `wiki/index.md`에 등록되어 있는지, 인덱스에 있는데 파일이 없는 항목은 없는지 확인한다.
3. 머리말이 규칙대로인지 확인한다.
4. 같은 주제를 다루는 문서가 둘 이상인지, 서로 모순되는 서술이 있는지 확인한다.
5. 발견 사항을 `log.md`에 `lint:` 항목으로 기록한다.
   - 링크 교정, 인덱스 누락 등록, 오탈자는 직접 고친다.
   - 내용 수정이 필요한 것은 고치지 않고 log에 제안으로만 남긴다.
6. git commit. 메시지: `lint: <날짜>`

## 7. 인덱스 형식 (`wiki/index.md`)

섹션(projects / concepts / lessons)별로, 문서당 한 줄만 쓴다.

```
- [[bc-card-bigdata-2026]] — BC카드 빅데이터 해커톤, 카드 결제 데이터 기반 상권 분석 (2026-08)
```

인덱스는 짧게 유지한다. 모든 작업이 인덱스를 먼저 읽으므로 이 파일이 길어지면 전체 비용이 오른다.

## 8. 로그 형식 (`log.md`)

맨 아래에 추가만 한다. 기존 항목은 고치지 않는다.

```
## 2026-09-22 ingest: bc-card-readme.md
- 신규: projects/bc-card-bigdata-2026
- 갱신: concepts/time-series-anomaly (이상탐지 절 추가)
- 도구: claude-code
```

## 9. 동시 작업 금지

두 컴퓨터 또는 두 도구가 동시에 위키를 고치면 충돌이 난다.
작업 시작 전 `git pull`, 끝나면 바로 `git commit` 후 `git push`한다.

## 10. 점검 스크립트와 노션 트래커

- 정기 점검은 `tools/lint.py`가 한다. 결정적 검사(링크·인덱스·머리말·sources)는 코드로, 판단 검사(중복·모순·원자료 대비 누락)는 OpenAI API로 하며, 결과는 `log.md`에 `lint:` 항목으로만 남기고 자동 수정은 하지 않는다.
- 대회 자체의 정보(기간, 주최, 상금, 결과, 상태)의 원본은 노션 트래커 "🏆 해커톤 - 현상금 사냥꾼"이다. 위키는 이 값을 project 문서 개요에 옮겨 적되, 출처를 "노션 트래커"라고 밝힌다.
- 반대로 "무엇을 만들었는가"의 원본은 위키다. 넣기가 끝나면 트래커의 해당 행에 프로젝트명, 기술 스택, 핵심 아이디어, 위키 문서 GitHub 링크(`https://github.com/accidentable/My_WIKI/blob/main/wiki/projects/<파일명>.md`)를 채운다. 위키 본문을 노션에 복사하지 않는다.

## 11. 학습 절 (concept 문서 필수)

이 위키의 목적은 기록이 아니라 **학습**이다. 해커톤에서 AI가 대신 짜고 넘어간 것을 나중에 스스로 설명할 수 있게 만드는 교재다. 모든 concept 문서는 본문 뒤에 `## 학습` 절을 가진다. 질문 수준은 **신입 개발자 기술면접의 프로젝트 딥다이브**에 맞춘다. 면접관은 "무엇을 썼나"가 아니라 "왜 그렇게 했고, 대안은 무엇이었고, 어디서 깨지는가"를 묻는다.

### 형식 (사이트가 파싱하므로 소제목과 굵은 표식을 그대로 지킨다)

```markdown
## 학습

### CS 주제
- 네트워크: 폴링 vs 푸시, HTTP keep-alive
- 운영체제: ...
(과목명: 세부 주제 형식. 1~3개. 머리말 cs_topics에도 과목명만 배열로 적는다)

### 설명할 수 있어야 하는 것
- 왜 이 방법을 골랐는가 (문제 → 선택 → 결과)
- 대안은 무엇이었고 무엇을 포기했는가
- 어떤 조건에서 이 방법이 깨지는가

### 확인 질문
1. **Q:** (L1 개념) X가 무엇이고 언제 쓰는가?
   **A:** 문서와 원자료 안에서 답할 수 있는 답. 3~5문장.
   **꼬리:** 면접관이 이어서 물을 한 단계 깊은 질문.
   **틀리기 쉬운 답:** 그럴듯하지만 틀린 답의 패턴 한 줄.
2. **Q:** (L2 판단) 이 프로젝트에서 왜 X를 택했고 Y 대신 쓴 이유는?
   ...
3. **Q:** (L3 한계) 규모가 10배가 되거나 Z 조건이 바뀌면 어디가 먼저 깨지는가?
   ...

### 더 파볼 것
- [자료 제목](URL) — 이 자료가 무엇을 채워주는지 한 줄
(1~3개. 실제로 열어 확인한 URL만. 교과서 장, 공식 문서, 검증된 면접 질문 저장소)
```

### 질문의 세 층
- **L1 개념**: 교과서 정의와 용도. "X가 뭐고 언제 쓰나." CS 과목과 이어지는 층.
- **L2 판단**: 이 프로젝트에서의 선택. "왜 X였나, 대안은, 트레이드오프는." 답에 문제 상황, 선택 이유, 결과가 들어가야 강한 답이다.
- **L3 한계·꼬리**: 깨지는 조건, 규모 변화, 장애 시 디버깅. 면접에서 꼬리질문으로 오는 층.

### 작성 규칙
- **자기 지식만으로 쓰지 않는다.** 질문을 만들기 전에 웹에서 같은 개념의 실제 면접 질문과 공식 문서를 찾아 대조한다. 검색으로 찾은 질문을 그대로 베끼지 말고, 이 프로젝트 맥락으로 바꾼다.
- 답(A)은 반드시 문서 본문이나 원자료(`raw/done/`)에서 뒷받침되어야 한다. 문서에 없는 사실로 답을 만들면 안 된다. 필요하면 본문을 먼저 보강한다.
- "틀리기 쉬운 답"은 실제로 흔한 오해여야 한다. 억지로 만들지 않는다.
- 질문 3개가 기본, 개념이 크면 5개까지. 6개 이상이면 concept를 쪼갠다.
- 점검(lint)은 각 Q의 A가 본문에서 뒷받침되는지 확인한다.

### 머리말 추가 필드
```
cs_topics: [네트워크, 운영체제]
```

## 12. 면접 준비 절 (project 문서 필수)

프로젝트 문서는 "무엇을 했나" 기록에 더해, 면접에서 이 프로젝트를 설명할 수 있게 하는 `## 면접 준비` 절을 가진다. 본문(개요~관련 문서) 뒤, 문서 맨 끝에 둔다. 사이트가 파싱하므로 소제목과 표 머리, 굵은 표식을 그대로 지킨다.

```markdown
## 면접 준비

### 한 문장 소개
(면접관에게 30초 안에 말할 한두 문장. 문제 → 무엇을 만들었나 → 결과)

### 기술 스택과 선택 이유
| 기술 | 역할 | 왜 이걸 썼나 | 대안과 포기한 것 |
|---|---|---|---|
| Next.js | 프론트+API | ... | ... |
(원자료에 이유가 없으면 "이유 자료에 없음"이라고 적는다. 지어내지 않는다. 3~8행)

### 고민한 점
- **무엇을 두고 고민했나** — 어떤 선택지가 있었고 무엇을 근거로 골랐나, 그 결과는. (3~5개. 원자료의 한계·반론·재측정 서술에서 뽑는다)

### 예상 질문
1. **Q:** (L1) ...
   **A:** ...
   **꼬리:** ...
   **틀리기 쉬운 답:** ...
2. **Q:** (L2) ...
3. **Q:** (L3) ...
(프로젝트 전체를 묻는 질문 3~5개. 개별 개념 질문은 concept 문서의 학습 절에 있으므로 중복하지 않는다. 형식은 11절과 같다)

### 솔직하게 말할 것
- AI 도구가 짠 부분, 이해가 얕은 부분, 검증 안 된 주장. 면접에서 부풀리면 꼬리질문에 무너지는 지점.
```

작성 규칙은 11절과 같다. 웹에서 같은 기술 스택·문제 유형의 실제 면접 질문을 찾아 대조하고, 답은 원자료로 뒷받침되는 것만 쓰며, 원자료의 확신 수준을 보존한다. 특히 "왜 이걸 썼나"는 원자료에 근거가 없으면 비워두는 것이 지어내는 것보다 낫다. 비워진 칸은 사용자가 직접 채울 자리다.

## 13. 문체

위키 문서는 사람이 읽는 글이므로 `~/.claude/skills/my-writing-voice/SKILL.md`의 규칙을 따른다. 요지만 옮기면 이렇다.

- 단언체("~했다", "~이다", "~것이다")를 쓰지 않고 격식 경어체("~했습니다", "~입니다")로 쓴다. 머리말과 표의 셀, 목록의 짧은 명사구는 예외다.
- "A가 아니라 B다" 같은 부정 후 반전, 짧게 끊어 힘을 주는 문장, 문단 끝의 멋 부린 한 줄, 문학적 비유는 쓰지 않는다.
- 구조·메커니즘·지점·측면·시사점 같은 추상 명사, 제시합니다·도출됩니다 같은 딱딱한 동사, ~을 통해·~에 있어서 같은 번역투, 또한·따라서·결론적으로로 시작하는 문장을 피한다.
- 질문에서 출발해 사례를 보여 주고 답으로 가는 순서로 쓰고, "처음에는 ~라고 생각했는데 ~해 보니 ~였습니다"처럼 판단이 바뀐 과정을 드러낸다. 숫자는 전후 한 쌍으로 쓴다.
- 주어 "저희는"을 쓰지 않는다. "~라고 생각합니다", "~로 보입니다"처럼 주어를 생략한다.
- 확인 질문의 Q는 면접관이 말하듯 "~인가요?", "~설명해 주시겠어요?"로, A는 지원자가 답하듯 "~했습니다, ~때문입니다"로 쓴다. 꼬리질문과 틀리기 쉬운 답도 같은 톤이다.

## 14. 아키텍처 절 (project 문서, 면접 준비 절 안)

프로젝트 문서의 `## 면접 준비` 안, `### 기술 스택과 선택 이유` 바로 뒤에 `### 아키텍처` 소절을 둔다. 사이트가 이 절을 읽어 그림으로 그리므로 형식을 정확히 지킨다.

```markdown
### 아키텍처
- [client] 웹 앱 :: Next.js, React :: Vercel
- [server] API 서버 :: FastAPI, Python :: Ncloud Ubuntu, Docker
- [data] 공시 DB :: SQLite, FTS5 :: 서버 로컬 디스크
- [external] LLM :: HyperCLOVA X HCX-005 :: 네이버 클라우드 API
- [ops] 스케줄러 :: APScheduler :: 서버 프로세스 내부
- 웹 앱 -> API 서버 :: HTTPS/JSON
- API 서버 -> 공시 DB :: SQL 조회
- API 서버 -> LLM :: function calling
```

- 노드 한 줄: `- [층] 이름 :: 기술(쉼표 구분) :: 호스팅 또는 실행 위치 :: 묶음(선택)`. 층은 client, edge, server, worker, data, chain, external, ops 중 하나. 네 번째 항목에 묶음 이름을 적으면 같은 묶음의 노드가 하나의 큰 상자 안에 그려진다. 예: `- [server] API :: FastAPI :: Docker :: EC2 인스턴스`, `- [server] 캐시 :: Redis :: Docker :: EC2 인스턴스`. 한 서버 안에서 같이 도는 것들을 묶을 때 쓴다.
- 연결 한 줄: `- 보내는 이름 -> 받는 이름 :: 무엇이 오가는지`. 이름은 노드 이름과 글자까지 같아야 한다.
- 호스팅이 원자료에 없으면 `호스팅 자료에 없음`이라고 쓴다. 지어내지 않는다. 노드는 3~9개.
- 기획서 단계라 구현이 없으면 계획된 구성을 그대로 적되, 첫 줄에 `- (계획 단계, 구현 없음)`을 넣는다.
